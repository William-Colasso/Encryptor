document.addEventListener('DOMContentLoaded', function() { //Quando o arquivo HTML for totalmente carregado, o comando será executado
    const botao1 = document.getElementById('botao1')
    botao1.addEventListener('click', function() {
       //Comando
    })

    const botao2 = document.getElementById('botao2')//Quando o arquivo HTML for totalmente carregado, o comando será executado
    botao2.addEventListener('click', function() {
        //Comando
    })
})